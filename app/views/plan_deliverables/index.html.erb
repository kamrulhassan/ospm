<%- model_class = PlanDeliverable.new.class -%>
<h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:start_date) %></th>
      <th><%= model_class.human_attribute_name(:end_date) %></th>
      <th><%= model_class.human_attribute_name(:plan_workpackage_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @plan_deliverables.each do |plan_deliverable| %>
      <tr>
        <td><%= link_to plan_deliverable.id, plan_deliverable_path(plan_deliverable) %></td>
        <td><%= plan_deliverable.description %></td>
        <td><%= plan_deliverable.start_date %></td>
        <td><%= plan_deliverable.end_date %></td>
        <td><%= plan_deliverable.plan_workpackage_id %></td>
        <td><%=l plan_deliverable.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_plan_deliverable_path(plan_deliverable), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      plan_deliverable_path(plan_deliverable),
                      :method => :delete,
                      :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_plan_deliverable_path,
            :class => 'btn btn-primary' %>
